# CLAUDE.md — Preflop Trainer

このファイルは Claude Code が毎回読む常設コンテキスト。仕様の一次情報はここ。
進捗とタスク分解は `plan.md` を参照。

**このファイルは薄く保つ。** 実装の細部を書き足さない（毎セッション読むコストになる）。
詳細は `docs/` に置き、必要なときだけ開く（§12）。

---

## 1. プロジェクトの目的

テキサスホールデムのプリフロップレンジを**反射で出せるレベルまで暗記する**ための自作ドリルアプリ。

背景として重要な制約：

- 学習者は電車内（片手・短時間・オフライン）で回す。**モバイルファーストは飾りではなく主要ユースケース**。
- 既存ツール（GTO Wizard 等）の無料枠は 2026 年時点で Practice 10 ハンド/日 程度に制限されており、必要な反復量（数千ハンド）に足りない。それが自作の動機。
- 目的は「正答率を上げること」ではなく「**判断が自動化されること**」。したがって正答率と同等に**反応時間**を計測する。

到達目標（v1 の成功条件）：

- 非グレーゾーンハンドの正答率 **95% 以上**
- 中央反応時間 **2.0 秒未満**
- 上記を 3 セッション連続で維持

---

## 2. 絶対ルール

### 2.1 レンジデータを生成しない

**Claude はレンジの数値を推測・生成・補完してはならない。** 例外なし。

- `data/ranges/*.json` の中身は、必ず人間が出典付きで入力する。
- 「AA から順に埋めておきますね」「一般的な UTG レンジは概ねこうです」といった補完は**禁止**。
- ハンドが欠けているデータを見つけた場合、埋めるのではなく **バリデータで警告を出す**。
- ユーザーからレンジ入力を依頼された場合は、出典（ツール名・フォーマット・レート・取得日）を先に確認する。

理由：わずかにズレたレンジを数千回反復するのは、何もしないより悪い。誤ったものを高速で自動化してしまう。

### 2.2 スコープを守る

以下は**このプロジェクトでは作らない**：

- ポストフロップソルバー（CFR 実装）→ TexasSolver / WASM Postflop を使う
- 実マネー・賭博に関わる一切の機能
- オンライン対戦、他プレイヤーとの通信
- ハンドヒストリの自動取り込み（v1 スコープ外）

### 2.3 用語の正確さ

- 採点で使う `freq_gap` を **EV ロスと呼ばない**。これは頻度差であって EV ではない。混同すると学習者が自分の実力を誤って見積もる。
- 正しい EV 比較にはソルバー出力が必要で、それは本プロジェクトの範囲外。

---

## 3. 技術スタック

| 項目 | 選択 | 理由 |
|---|---|---|
| ビルド | Vite | 軽量・設定が薄い |
| 言語 | TypeScript (strict) | レンジデータの型崩れを静的に防ぐ |
| UI | React 18 | 状態管理が素直 |
| スタイル | Tailwind CSS | モバイル対応の記述量が少ない |
| 状態管理 | React state + `useReducer` | 外部ライブラリ不要 |
| 永続化 | `localStorage` | バックエンドなし。オフラインで動く |
| デプロイ | GitHub Pages (Actions) | 無料・静的・スマホからアクセス可 |
| テスト | Vitest | 採点ロジックとバリデータのみ対象 |

**サーバーは持たない。** 全データは静的 JSON としてバンドルする。オフラインで完全動作すること。

---

## 4. ディレクトリ構成

```
preflop-trainer/
├── CLAUDE.md
├── plan.md
├── data/
│   ├── ranges/
│   │   ├── 6max_100bb_rfi_utg.json
│   │   ├── 6max_100bb_rfi_hj.json
│   │   ├── 6max_100bb_rfi_co.json
│   │   ├── 6max_100bb_rfi_btn.json
│   │   └── 6max_100bb_rfi_sb.json
│   └── README.md          # データ入力手順と出典の記録
├── scripts/
│   └── validate-ranges.ts # CI でも走らせる
├── src/
│   ├── domain/            # 純粋関数のみ。React 非依存
│   │   ├── hands.ts       # 169 ハンドの生成・正規化・パース
│   │   ├── scoring.ts     # 採点ロジック
│   │   ├── sampler.ts     # 重み付き出題
│   │   └── stats.ts       # 集計
│   ├── storage/
│   │   └── local.ts       # localStorage 読み書き + import/export
│   ├── components/
│   ├── screens/
│   │   ├── Drill.tsx
│   │   ├── Stats.tsx
│   │   └── LeakGrid.tsx
│   └── main.tsx
└── tests/
```

**`src/domain/` は React に依存させない。** 採点・サンプリング・集計は純粋関数として単体テスト可能に保つ。ここが将来ポストフロップドリルやハンド台帳に再利用される部分。

---

## 5. ハンド表記規約

- ランク順：`A K Q J T 9 8 7 6 5 4 3 2`（`10` ではなく `T`）
- 常に**高いランクを先に**書く：`AKs`（`KAs` は不正）
- ペア：サフィックスなし（`AA`, `77`）
- スーテッド：`s`（`AKs`）
- オフスート：`o`（`AKo`）
- 全 169 通り：ペア 13 + スーテッド 78 + オフスート 78

`hands.ts` は以下を提供する：

```ts
export const ALL_HANDS: Hand[];              // 169 個。標準順で固定
export function normalizeHand(s: string): Hand;
export function handToGridPos(h: Hand): { row: number; col: number };
export function comboCount(h: Hand): number; // ペア6 / スーテッド4 / オフスート12
```

`comboCount` は将来のコンボ数重み付けサンプリングに使う。v0.1 では使わなくてよいが実装しておく。

---

## 6. データモデル

### 6.1 レンジチャート JSON

```json
{
  "schema_version": 1,
  "id": "6max_100bb_rfi_btn",
  "format": "6max",
  "stack_bb": 100,
  "spot": "RFI",
  "hero_position": "BTN",
  "villain_position": null,
  "actions": ["raise", "fold"],
  "source": {
    "name": "<ツール名 / フォーマット / レート>",
    "url": "<URL>",
    "retrieved_at": "2026-08-15",
    "entered_by": "juno",
    "notes": "<サイズ設定など>"
  },
  "ranges": {
    "AA": { "raise": 1.0, "fold": 0.0 },
    "K9o": { "raise": 0.32, "fold": 0.68 }
  }
}
```

### 6.2 バリデーション規則（`scripts/validate-ranges.ts`）

すべて**エラー**（ビルドを落とす）：

1. `ranges` のキーが正規表記でない
2. 各ハンドの頻度合計が `1.0 ± 0.01` から外れる
3. `actions` に無いアクションが `ranges` に現れる
4. `source.name` が空

**警告**（ビルドは通す）：

5. 169 ハンドすべてが揃っていない → 欠損は `fold: 1.0` として扱うが、件数を報告する

### 6.3 ポジション定義

6max のポジション：`UTG`, `HJ`, `CO`, `BTN`, `SB`, `BB`

**RFI チャートは 5 つのみ**（`UTG`, `HJ`, `CO`, `BTN`, `SB`）。BB は全員フォールド時にオープンする状況が存在しないため、RFI チャートを作らない。

### 6.4 セッション記録

```ts
type Attempt = {
  ts: number;            // epoch ms
  chartId: string;
  hand: Hand;
  chosen: Action;
  correct: boolean;      // strict 判定（グレーゾーンは別集計）
  isGray: boolean;
  freqGap: number;       // 0.0–1.0
  responseMs: number;
};
```

`localStorage` キー：`preflop-trainer:v1:attempts`、`preflop-trainer:v1:weights`

Attempt は全件保持（1 件 ~80 bytes、10 万件でも 8MB 未満だが、5 万件を超えたら古いものから圧縮集計に落とす）。

---

## 7. 採点仕様

出題は「チャート内のあるハンド」。ユーザーはアクションを 1 つ選ぶ。

```
chosenFreq  = ranges[hand][chosen]
maxFreq     = max(ranges[hand][a] for a in actions)
secondFreq  = 2番目に大きい頻度
freqGap     = maxFreq - chosenFreq
isGray      = (maxFreq - secondFreq) < 0.15
correct     = (chosen == argmax)
```

### 表示指標

| 指標 | 定義 | 位置づけ |
|---|---|---|
| **正答率** | 非グレーハンドのみの `correct` 率 | **主指標**。95% を目指す |
| グレーゾーン正答率 | グレーハンドのみ | 参考。ここは低くても問題ない |
| 平均 freqGap | 全ハンドの `freqGap` 平均 | 誤りの深刻度。0.15 未満が目安 |
| 中央反応時間 | `responseMs` の中央値 | **主指標**。2.0 秒未満 |

**混合戦略のハンドを間違いとして強く罰しない。** GTO レンジには 50/50 に近い頻度が実在し、そこを 100% 当てさせるのは学習として無意味。だからグレーゾーンを分離する。

### フィードバック

回答直後に必ず表示する：

- 選んだアクションとその頻度
- 全アクションの頻度内訳（バーで可視化）
- グレーゾーンの場合はその旨を明示（「ここは混合。どちらでも大きな損はない」）

---

## 8. 出題サンプリング仕様（`sampler.ts`）

各 `(chartId, hand)` ペアが重み `w` を持つ。初期値 `1.0`。

```
不正解 → w = min(w * 3.0, 12.0)
正解   → w = max(w * 0.6, 0.25)
```

- 出題は `w` に比例した確率で抽選
- **クールダウン**：直近 8 問に出たハンドは再抽選対象から除外
- 反応時間が 5 秒を超えた正解は「遅い正解」として `w = w * 1.0`（減衰させない）

重みは `localStorage` に永続化。リセット機能を用意する。

---

## 9. UI 規約

**モバイルファースト。 375px 幅で完全に成立させてから、デスクトップに広げる。**

- アクションボタンは**画面下部**、親指の届く範囲。最小タップ領域 48px。
- 片手操作で完結すること。ドリル中にスクロールを要求しない。
- 反応時間を測るので、**問題表示からボタン表示までのアニメーションを入れない**。即座に操作可能にする。
- ダークを既定にする（電車内で使う）。
- デスクトップではキーボードショートカット：`R`=raise, `C`=call, `F`=fold, `3`=3bet。
- リークグリッドは 13×13 のヒートマップ（縦横ともランク降順、右上スーテッド／左下オフスート＝標準レイアウト）。色は誤答率で連続的に変化させる。
- 色だけに情報を載せない。数値ラベルを併記する。

コピーの原則：能動態・文末は簡潔に。「送信」ではなく具体的な動作名を使う。空状態は次にやることを示す。

---

## 10. コーディング規約

- TypeScript `strict: true`。`any` 禁止。
- ドメイン層は副作用なし。`Date.now()` や `Math.random()` は引数で注入する（テスト可能性のため）。
- テストは `domain/` と `scripts/validate-ranges.ts` に対してのみ書く。UI のテストは不要。
- コード内の識別子・コメントは英語。ドキュメントと UI テキストは日本語。
- コミットメッセージは英語、Conventional Commits 準拠。

---

## 11. 将来の拡張（v1 では実装しない）

順序は `plan.md` に従う。設計時に閉ざさないでおくこと：

- `spot` フィールドで `vs_RFI` / `vs_3bet` / `push_fold` を表現できる構造
- スタックサイズ別チャート（`stack_bb`）
- 実戦ハンドの記録台帳（判断理由を先に書き、後からソルバーで答え合わせする）
- セッション統計の JSON エクスポート（Notion へ手動転記する用）

---

## 12. 詳細ドキュメント（必要なときだけ開く）

| ファイル | 内容 | 開くタイミング |
|---|---|---|
| `docs/architecture.md` | レイヤ構成、データ読み込み、状態機械、永続化のタイミング | 構造に触るとき |
| `docs/domain-contracts.md` | `src/domain/` の確定シグネチャと境界条件、テストケース一覧 | ドメイン層を実装・修正するとき |
| `docs/workflow.md` | モデル分担とサブエージェントへの依頼テンプレート | 作業を分割するとき |
| `data/README.md` | レンジ入力手順と出典の記録 | データを入れるとき |

`docs/domain-contracts.md` は §7 / §8 の仕様を実装可能な粒度まで詰めたもの
（同率トップの扱い、グレー境界の浮動小数、クールダウンが候補を全滅させる場合など）。
**ここと `CLAUDE.md` が矛盾したら `CLAUDE.md` が正**。矛盾を見つけたら先に `CLAUDE.md` を直す。

## 13. モデル分担（トークン節約）

詳細は `docs/workflow.md`。要点だけ：

- **Opus**：仕様解釈、境界条件の確定、型設計、レビュー、`CLAUDE.md` / `plan.md` の変更判断
- **Sonnet**（サブエージェント）：確定済み契約どおりの実装、テスト、設定ファイル、雛形
- サブエージェントには**読ませる範囲と書かせるファイルを明示**し、迷ったら実装させず質問させる
- レンジデータの入力はどのモデルにも投げない（§2.1）

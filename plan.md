# plan.md — Preflop Trainer

仕様の一次情報は `CLAUDE.md`。ここには**やる順番と完了条件**だけを書く。

---

## 現在地

- [x] v0.0 設計（`docs/architecture.md`, `docs/domain-contracts.md`, `docs/workflow.md`, `data/README.md`）
- [ ] v0.1 RFI ドリル（MVP）
- [ ] v0.2 統計とリークグリッド
- [ ] v0.3 vs RFI（コール / 3bet / フォールド）
- [ ] v0.4 プッシュ / フォールド
- [ ] v0.5 エクスポートと実戦台帳

---

## v0.1 — RFI ドリル（MVP）

**狙い**：今日中に「電車で回せるもの」を動かす。機能は削れるだけ削る。

### 完了条件

- [ ] `data/ranges/` に 5 チャート（UTG / HJ / CO / BTN / SB、6max 100bb RFI）が出典付きで存在する
- [ ] `npm run validate` が全チャートを通す
- [ ] スマホのブラウザで 100 問連続でドリルできる
- [ ] 誤答したハンドが再出題される（体感で確認できる）
- [ ] リロードしても重みとセッション統計が消えない
- [ ] GitHub Pages にデプロイされ、スマホからアクセスできる

### タスク

**1. 足場**
- [ ] Vite + React + TS + Tailwind でプロジェクト初期化
- [ ] `tsconfig` を `strict: true` に
- [ ] Vitest を導入

**2. ドメイン層（先に書く。UI より前）**

> シグネチャと境界条件は `docs/domain-contracts.md` で確定済み。実装時はそれを見る。

- [ ] `types.ts`：`Hand` / `Action` / `RangeChart` / `Attempt`
- [ ] `hands.ts`：`ALL_HANDS` の生成、`normalizeHand`、`handToGridPos`、`comboCount`
- [ ] `hands.test.ts`：169 個であること、`KAs` を弾くこと、グリッド座標が重複しないこと
- [ ] `scoring.ts`：`freqGap` / `isGray` / `correct` の算出
- [ ] `scoring.test.ts`：純粋レンジ、混合レンジ、境界（`maxFreq - secondFreq == 0.15`）
- [ ] `sampler.ts`：重み更新と重み付き抽選、クールダウン（乱数は注入）
- [ ] `sampler.test.ts`：シード固定で分布を検証、直近 8 問に同一ハンドが出ないこと
- [ ] `validateChart.ts` + テスト（`scripts/validate-ranges.ts` と CI が共用する純粋関数）

**3. データ**
- [ ] `data/README.md` に入力手順を書く（出典の記録方法を含む）
- [ ] `scripts/validate-ranges.ts` を実装
- [ ] **BTN のチャートを 1 つだけ手入力する**（残り 4 つは後回し。1 チャートあれば動作確認できる）
- [ ] 残り 4 チャートを入力

> ⚠️ レンジの数値は Claude が生成しない。出典から人間が入力する（`CLAUDE.md` §2.1）。

**4. 永続化**
- [ ] `storage/local.ts`：attempts と weights の読み書き
- [ ] スキーマバージョンを持たせ、不一致時は捨てて初期化する

**5. UI（最小）**
- [ ] `Drill.tsx`：ハンド表示 → アクション選択 → 頻度内訳のフィードバック → 次の問題
- [ ] チャート選択（5 つのうち 1 つ、または「全部混ぜる」）
- [ ] セッション中の簡易カウンタ（正答数 / 出題数 / 中央反応時間）
- [ ] 375px で成立することを実機かデバイスエミュレータで確認

**6. デプロイ**
- [ ] GitHub Actions で Pages にデプロイ
- [ ] `vite.config.ts` の `base` をリポジトリ名に合わせる（Pages でよく踏む）
- [ ] `vite-plugin-pwa` を入れる（**オフライン必須**。地下鉄で開けないと主要ユースケースが死ぬ）
- [ ] スマホのホーム画面に追加し、**機内モードで**動くか確認

### v0.1 で意図的にやらないこと

- 統計画面、リークグリッド
- エクスポート / インポート
- キーボードショートカット
- ダークモード切り替え（最初からダーク固定でよい）
- アニメーション

---

## v0.2 — 統計とリークグリッド

**狙い**：「上達を表現する」層。ここが本来の目的なので、v0.1 の直後に必ずやる。

### 完了条件

- [ ] 13×13 のヒートマップで、誤答率の高いハンドが一目で分かる
- [ ] 正答率・平均 freqGap・中央反応時間が、直近 7 日 / 全期間で見られる
- [ ] チャート別（ポジション別）に正答率が出る

### タスク

- [ ] `stats.ts`：期間フィルタ、グレー / 非グレー分離集計、チャート別集計
- [ ] `stats.test.ts`
- [ ] `LeakGrid.tsx`：誤答率のヒートマップ。色 + 数値ラベル。タップでそのハンドの履歴を表示
- [ ] `Stats.tsx`：主指標 2 つ（正答率・中央反応時間）を最上部に置く
- [ ] 反応時間の分布（ヒストグラム）。平均ではなく分布を見る

### 判断ポイント

正答率 95% / 中央 2 秒を達成したら **v0.1 のチャートは「完了」扱いにして v0.3 へ進む**。そこから先の精度向上は投資対効果が落ちる。

---

## v0.3 — vs RFI（コール / 3bet / フォールド）

**狙い**：オープンレンジだけでは実戦の半分もカバーできない。相手のオープンに対する反応が次の必須知識。

### 完了条件

- [ ] `spot: "vs_RFI"` のチャートが読み込める（`villain_position` が機能する）
- [ ] アクションが 3 択（`3bet` / `call` / `fold`）で動作する
- [ ] 出題時に「誰がどのポジションからオープンしたか」が明示される

### タスク

- [ ] データモデルの `actions` 可変長対応を確認（v0.1 の設計で通るはずだが要検証）
- [ ] UI のアクションボタンをアクション数に応じて可変に
- [ ] 対戦状況の表示コンポーネント（テーブル図か、テキストで十分か判断する）
- [ ] チャート入力：まず BTN vs CO open、BB vs BTN open の 2 つ（頻出順）
- [ ] 段階的に拡張

> このスポットはチャート数が組み合わせで増える（オープン位置 × 自分の位置）。**全部作ろうとしない。** 出現頻度の高い順に上位 6〜8 個で止める。

---

## v0.4 — プッシュ / フォールド

**狙い**：トーナメントのインマネに直結する層。習得コストが低く、結果への寄与が大きい。

### 完了条件

- [ ] `stack_bb` が 10 / 12 / 15 / 20 のチャートを切り替えられる
- [ ] アクションが `push` / `fold` の 2 択で動作する
- [ ] スタックサイズがランダムに変わるミックスモードがある

### タスク

- [ ] `stack_bb` によるチャート選択 UI
- [ ] ミックス出題モード（実戦ではスタックが刻々変わるため、固定スタックだけの練習は不十分）
- [ ] チャート入力（Nash 均衡のプッシュフォールド表は公開されているものが多い。出典必須）

### 注意

ICM 補正の入ったチャートと、チップ EV ベースのチャートを**混ぜない**。JSON の `source.notes` にどちらか明記する。混ざると学習が壊れる。

---

## v0.5 — エクスポートと実戦台帳

**狙い**：ドリルの外側、実戦ハンドの検討ログ。学習の主指標が「自力で漏れを見つけられた割合」に移る段階。

### 完了条件

- [ ] セッション統計を JSON / CSV でエクスポートできる
- [ ] 実戦ハンドを記録できる：状況、自分の選択、**選択理由を先に記述**、後からソルバー確認結果を追記
- [ ] 「ソルバー確認前に自分で漏れを指摘できたか」のフラグが立てられる

### 設計上の注意

これはドリルとは別モードにする。ドリルは高速反復、台帳は低速熟考。**同じ画面に混ぜない。**

---

## 詰まったときの参照先

- ポストフロップの検討が必要になったら：TexasSolver（ローカル、無料）または WASM Postflop（ブラウザ、無料）
- レンジデータの出典は `data/README.md` に一元管理する
- 仕様の疑問は `CLAUDE.md` を先に読む。矛盾を見つけたら `CLAUDE.md` を直してからコードを書く
